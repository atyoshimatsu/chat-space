.side-bar
  .side-bar__user-name
    .top-items
      .top-items__name
        = current_user.name
      .top-items__icons
        = link_to new_group_path, class: "top-items__icons__btn-edit" do
          = icon('far', 'edit')
        = link_to edit_user_path(current_user), class: "top-items__icons__btn-cog" do
          = icon('fa', 'cog')
  .side-bar__group-list
    .group-info
      - current_user.groups.each do |group|
        = link_to "#", class: "group-info__name" do
          = group.name
        .group-info__message
          まだメッセージはありません